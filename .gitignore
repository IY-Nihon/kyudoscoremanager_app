# ── 依存パッケージ ──
node_modules/

# ── ビルド成果物 ──
.expo/
dist/
dist-stg/
web-build/
expo-env.d.ts
.firebase/
*.tsbuildinfo

# ── ネイティブ（自動生成 / 署名ファイル） ──
/ios
/android
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision

# ── Metro ──
.metro-health-check*

# ── ログ・診断 ──
*.log
*.heapsnapshot
*.trace
npm-debug.*
yarn-debug.*
yarn-error.*
diagnostic_*
prebuild_log.txt
RecordAppExpo

# ── 機密情報（絶対にコミットしない） ──
.env
.env*.local
users.csv
users.json
auth_test_result.txt
test_auth.*

# ── バックアップ / リストア検証（ローカル作業用） ──
backup-output/
backup-output-old/
restore-test/
restore-*.mjs
*.json.gz

# ── 過去のデバッグ資材を退避した場所（Git管理外） ──
_archive/
scratch/

# ── OS / エディタ ──
.DS_Store
Thumbs.db

# Playwright の実行結果（毎回作り直される）
test-results/
playwright-report/
e2e/.auth/
dist2/
dist3/
