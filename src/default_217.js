/**
 * Library Bridge: default_217.js (react-native / Text)
 *
 * 元はソースマップから復元された react-native の Text 実装。
 * Web ビルドでは Metro が react-native-web へ解決する（両方に Text が存在することを確認済み）。
 *
 * 利用側は default import（`X.default`）で参照するため、__esModule + default を明示する。
 */
"use strict";

const { Text } = require('react-native');

Object.defineProperty(exports, '__esModule', { value: true });
exports.default = Text;
module.exports.Text = Text;
