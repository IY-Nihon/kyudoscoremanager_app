/**
 * Library Bridge: module_696.js (date-fns)
 *
 * 元はソースマップから復元された date-fns のバレルファイルで、
 * module_697 以降の関数群（約300ファイル）を再エクスポートしていた。
 * package.json に date-fns@^4.1.0 があるため npm パッケージへ委譲する。
 *
 * 利用箇所: src/JP_CustomCalendarModal_695.js
 * 使用API : getDate, getDaysInMonth, getMonth, getYear, setDate, setMonth, setYear
 */
"use strict";

module.exports = require('date-fns');
