/**
 * Library Bridge: combine_201.js (zustand/middleware)
 *
 * 元はソースマップから復元された zustand のミドルウェア群。
 * package.json に zustand@^5.0.12 があるため npm パッケージへ委譲する。
 *
 * 利用箇所: JP_useScoreStore_174
 * 使用API : persist, createJSONStorage
 */
"use strict";

module.exports = require('zustand/middleware');
