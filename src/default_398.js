/**
 * Library Bridge: default_398.js (react-native / TextInput)
 *
 * 元はソースマップから復元された react-native の TextInput 実装。
 * Web ビルドでは Metro が react-native-web へ解決する（両方に TextInput が存在することを確認済み）。
 *
 * 利用側は default import（`X.default`）で参照するため、__esModule + default を明示する。
 */
"use strict";

const { TextInput } = require('react-native');

Object.defineProperty(exports, '__esModule', { value: true });
exports.default = TextInput;
module.exports.TextInput = TextInput;
