/**
 * Library Bridge: default_1001.js (react-native-svg)
 *
 * 元はソースマップから復元された react-native-svg のバレルファイル。
 * package.json に react-native-svg@15.15.3 があるため npm パッケージへ委譲する。
 *
 * 利用箇所: JP_AnalysisScreen_1000（Circle, Line, Path, Text を使用）
 * 同じパッケージは src/KyudoBackgroundAnimation.js が既に直接 require しており動作実績あり。
 */
"use strict";

module.exports = require('react-native-svg');
