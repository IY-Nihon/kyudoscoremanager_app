# 実装計画：本番環境との完全パリティ（設定・分析画面の復元）

ローカルの開発環境を、提供された本番ソースコード（`RecordAppExpo`）と100%一致させます。前回の修正で「本番にないもの」として削除しすぎた可能性のある箇所（管理者モードのメンバー表示など）を、本番コードを正として復元します。

## ユーザーレビューが必要な事項
- **管理者モードの挙動**: メンバーログイン時でも「管理者モード」トグルを表示し、パスワード認証を経て管理者権限に昇格（個人IDの表示など）できるフローを復元します。これは本番環境（`RecordAppExpo`）と同一の仕様であることを確認済みです。
- **メンバーリストのソート順**: 本番コードでは「学年 -> 性別 -> 五十音」の順でソートされています。ローカル環境もこれに合わせます。
- **分析画面のトグル**: 既に復元済みであることを確認しました。

## 修正内容

---

### 1. 設定画面 ([JP_SettingsScreen_1023.js](file:///C:/Users/yutoi/Documents/%E5%BE%A9%E5%85%83%E3%82%A2%E3%83%97%E3%83%AA/AppBase/src/JP_SettingsScreen_1023.js))

#### [MODIFY]
- **管理者モードトグルの表示条件**: メンバーログイン時（`V === 'member'`）でも非表示にせず、表示するように戻します。
- **パスワード認証モーダル**: メンバーがトグルをオンにした際、`verifyGroupPassword` を呼び出す認証モーダルを確実に動作させます。
- **サポートボタンの追加**: 画面下部（ログアウトボタンの下など）に、本番仕様の「運用ガイド・ヘルプ」ボタン（赤色背景）を追加します。
- **タグ入力プレースホルダー**: 「新しいタグを追加」に統一します。

---

### 2. 分析画面 ([JP_AnalysisScreen_1000.js](file:///C:/Users/yutoi/Documents/%E5%BE%A9%E5%85%83%E3%82%A2%E3%83%97%E3%83%AA/AppBase/src/JP_AnalysisScreen_1000.js))

#### [CONFIRMED]
- **卒業生表示トグルの存在**: 既に本番同様のロジック（`'member' !== k` での表示制御）が実装されていることを確認しました。追加の修正は不要です。

---

### 3. メンバー管理画面 ([JP_MemberScreen_1022.js](file:///C:/Users/yutoi/Documents/%E5%BE%A9%E5%85%83%E3%82%A2%E3%83%97%E3%83%AA/AppBase/src/JP_MemberScreen_1022.js))

#### [MODIFY]
- **ソートロジックの修正**: 一部誤って削除していた「性別順」のソートロジックを復元し、本番コードと完全に一致（学年 -> 性別 -> 五十音）させます。

---

## 検証計画

### 自動テスト（ブラウザツールによる動作確認）
- 設定画面を開き、メンバーログイン状態で「管理者モード」トグルが表示されるか確認。
- トグルをオンにした際、パスワード入力モーダルが表示されるか確認。
- 分析画面を開き、団体ログイン状態で「卒業生を表示」トグルが存在するか確認。

### 手動確認事項
- サポートボタンのデザイン（赤色背景、シャドウ）が本番と遜色ないか。
- メンバーリストの並び順が、学年・性別・名前の順で一貫しているか。
