# 複数人比較および学年別メンバーアコーディオンの実装計画

他のメンバーとの比較機能を複数人との同時比較に対応させ、記録画面のメンバー選択において学年やその他/ゲストごとに折りたたんで表示（アコーディオン）できるように改善します。

## ユーザー確認事項
- 複数人の同時比較では、基準メンバー（青）に加え、比較対象（赤、緑、オレンジ、紫など）をカラーパレットから順に色分けしてプロットします。
- 矢所の横並べ比較は、人数が増えてもレイアウトが崩れないよう、横スクロール可能なビュー（`ScrollView`）を用いて並べて表示します。
- 現役メンバーの折りたたみは、学年（1〜4年）およびその他（ゲスト）のグループに分け、初期状態はすべて展開された状態（オープン）とします。

## Proposed Changes

### [Component: Analysis]
個人分析モーダルにおいて、複数人のメンバーを選択・比較できるようにします。

#### [MODIFY] [JP_AnalysisScreen_1000.js](file:///c:/Users/yutoi/Documents/kyudoscoremanager_app/src/JP_AnalysisScreen_1000.js)
- `compareMember` (単一) ステートを `compareMembers` (配列) ステートに変更。
- `CompareGraph` を拡張し、基準メンバー ＋ 選択された複数メンバーの的中率推移を異なる色で重ね合わせて描画。
- メンバー選択ポップアップで、トグル方式（タップで追加・削除）に変更し、選択済みのメンバーに「✓」を表示するUIを追加。
- 矢所の比較部を `ScrollView` による横並びスクロールに変更し、複数メンバーの的をコンパクトに並べて表示。
- 立ち順別的中率の表示部をループ処理で複数メンバー分の進捗バーを並べるように変更。

---

### [Component: Record Archer Selection]
記録画面の射手選択モーダルにおいて、メンバーリストを学年やその他で折りたためるようにします。

#### [MODIFY] [JP_ArcherActionModal_689_fixed.js](file:///c:/Users/yutoi/Documents/kyudoscoremanager_app/src/JP_ArcherActionModal_689_fixed.js)
- 現役メンバー `L` を `grade` ごとにグループ化する `activeGroups` の `useMemo` を追加（1年、2年、3年、4年、その他/ゲスト）。
- 各学年グループの展開・非展開を管理する `expandedActiveGrades` ステートを追加（初期値は全展開）。
- 卒業生の期アコーディオンと同様のスタイルで、各学年のヘッダーと開閉用アイコンを描画。開いている場合のみメンバーリストを展開する。

---

## Verification Plan

### Automated Tests
- `npx expo export --platform web` コマンドでWeb書き出しを行い、ビルドがエラーなく正常に完了し、コードの構文エラーやコンパイルエラーが無いことを確認します。

### Manual Verification
- アプリ上で「分析」画面を開き、メンバー詳細モーダルの「他のメンバーと比較」ボタンから複数のメンバーを選択し、グラフが重ね合わせ描画されるか、的が横並びでスクロール表示されるか、立ち順的中率が並んで表示されるかを確認。
- 記録画面で射手をタップし、学年ごとにメンバーがアコーディオンでまとめられて開閉できるかを確認。
