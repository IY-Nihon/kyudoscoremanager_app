# 使い終わった script

一度きりの移行や修理に使ったもの。いまの倉庫では動かないものもある。消さずに置いてあるのは、
同じ種類の作業がまた要るときに手順の参考にするため。

- `migrate-in-browser.js` / `recover-unsynced-in-browser.js` … ブラウザの console に貼って動かした移行・復旧
- `patch_swift6_final.*` / `SwiftUIHostingView_before_temp.swift` … iOS の Swift 6 対応で当てたパッチと元の控え
