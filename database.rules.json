{
  "rules": {
    "live_sessions": {
      "$groupId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "live_history": {
      "$groupId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
